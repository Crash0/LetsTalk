<core:UserControlViewBase x:Class="LetsTalk.Client.AgentClient.Views.PhoneView"
xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LetsTalk.Client.AgentClient.Views"
             xmlns:core="http://www.Crash0.com/Core"
             xmlns:vm="clr-namespace:LetsTalk.Client.AgentClient.ViewModels"
             mc:Ignorable="d"  Width="300"
             d:DesignHeight="400" d:DesignWidth="300"
             d:DataContext="{d:DesignInstance Type=vm:PhoneViewModel }">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" DataContext="{Binding Caller}">
            <TextBox Text="{Binding CallerName}"></TextBox>
            <TextBox Text="{Binding CallerId}"></TextBox>
        </Grid>
        <Button Grid.Row="1" Content="connect" Command="{Binding ConnectCommand}"></Button>
        
    </Grid>
    </core:UserControlViewBase>