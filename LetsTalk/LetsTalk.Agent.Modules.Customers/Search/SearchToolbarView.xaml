<UserControl x:Class="LetsTalk.Agent.Modules.Customers.Search.SearchToolbarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LetsTalk.Agent.Modules.Customers.Search"
             xmlns:controls="http://www.Crash0.com/Core"
             mc:Ignorable="d" d:DesignHeight="30" d:DesignWidth="200"
             >
    <UserControl.Resources>
        <Style TargetType="TextBox">
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Background" Value="Transparent"></Setter>
        </Style>
        <Style TargetType="Border">
            <Setter Property="BorderBrush" Value="Black"></Setter>
            <Setter Property="CornerRadius" Value="20"></Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50"/>
            <ColumnDefinition Width="149*"/>
        </Grid.ColumnDefinitions>
        <controls:RoundedBox Grid.ColumnSpan="2"/>
        <Label VerticalAlignment="Center" Height="26" Margin="0,2">Search</Label>
        <TextBox  VerticalAlignment="Center" Grid.Column="1"  Margin="0,6" Height="18" TextDecorations="Underline"/>
    </Grid>
</UserControl>

